"use client"

/*
    here to be a map + button
    when the user clicks on the button the let me access your location premmesion appears
    then the [lng, lat] should be set in the URL using setSearchParams() hook ?
    then send it to the API
    then shows the results on the map 
    is it like this ?
*/
export default function NearestRoasters() {
    return (
        <div>
            <h2>the nearest roasters to your location (500km - 1000km)... isn&apos;t large area ?</h2>
        </div>
    )
}

